## Checkpoint 7 – Linux Investigation

The Linux investigation was performed using a KillerCoda Playground.

### Operating System

I used the following command:

```bash
cat /etc/os-release
The command was used to identify the Linux distribution and operating system version.

```bash
lscpu
This command displays information about the CPU, including architecture, processor information, and available CPUs.

```bash
free -h
This command displays memory usage in a human-readable format.

```bash
df -h
This command displays disk space usage for the mounted filesystems.

If this Linux server were migrated to the cloud, which AWS, Azure, and GCP services could host it?
-it could be hosted using virtual machine services from AWS, Azure, and Google Cloud.

AWS
A suitable service would be Amazon EC2, which provides virtual machines that can run Linux operating systems.

Azure
A suitable service would be Azure Virtual Machines, which provides virtualized computing resources capable of running Linux.

Google Cloud
A suitable service would be Compute Engine, which provides configurable virtual machines that can run Linux.
These services are comparable because they provide cloud-based virtual computing environments where a Linux operating system can run.
